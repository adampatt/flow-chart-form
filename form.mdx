Ensure the function in actions takes in the correct values using values from the types. If FormData is being used
remove it. 

Remove all local state and use formState within the form.

Within the onSubmit, if necessary transform the submitted values to match the values required by the action. Ensure that
the action is being called with the correct values and is typesafe using the types.

Use shadcn form format for the form. 

Any types created should be made in the types file and exported then used in the relevant files.